{
  "name": "omnitransfer-mobile",
  "version": "0.1.0",
  "description": "OmniTransfer Capacitor mobile app",
  "private": true,
  "scripts": {
    "build": "npm run build:web && npx cap sync",
    "build:web": "cd ../desktop && pnpm build",
    "android": "npx cap open android",
    "ios": "npx cap open ios",
    "sync": "npx cap sync",
    "update": "npx cap update"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "~5.4.0"
  }
}
